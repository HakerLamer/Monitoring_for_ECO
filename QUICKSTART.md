# ⚡ Быстрый старт проекта "Мониторинг экосистемы"

## 🎯 Что это?

Готовое решение для мониторинга экосистемы с использованием:
- 🎥 **YOLOv8** - обнаружение животных на видео
- 🎵 **YAMNet** - распознавание звуков природы
- 📊 **Визуализация** - графики и дашборды
- 🚨 **Оповещения** - автоматические уведомления

## 📦 Установка за 5 минут

### Шаг 1: Распаковка проекта

```bash
# Если скачали архив
tar -xzf ecosystem_monitor.tar.gz
cd ecosystem_monitor

# Или просто перейдите в папку проекта
cd ecosystem_monitor
```

### Шаг 2: Создание виртуального окружения

```bash
# Linux/Mac
python3 -m venv venv
source venv/bin/activate

# Windows
python -m venv venv
venv\Scripts\activate
```

### Шаг 3: Установка библиотек

```bash
pip install -r requirements.txt
```

⏱️ **Время установки:** 5-10 минут (зависит от скорости интернета)

### Шаг 4: Запуск Jupyter

```bash
jupyter notebook
```

Автоматически откроется браузер с Jupyter.

## 🚀 Первый запуск

### Откройте notebooks по порядку:

#### 1️⃣ **01_ecosystem_monitoring.ipynb**
**Что внутри:**
- Загрузка модели YOLOv8
- Создание демо-видео
- Анализ видео с обнаружением животных
- Статистика детекций

**Время выполнения:** 3-5 минут

**Ключевые ячейки:**
```python
# Загрузка модели
model = YOLO('yolov8n.pt')  # При первом запуске скачает модель

# Создание анализатора
video_analyzer = VideoAnalyzer(model)

# Анализ видео
results = video_analyzer.analyze_video('data/videos/demo_wildlife.mp4')
```

#### 2️⃣ **02_audio_analysis.ipynb**
**Что внутри:**
- Загрузка модели YAMNet
- Создание демо-аудио
- Анализ звуков природы
- Спектрограммы и визуализация

**Время выполнения:** 2-3 минуты

**Ключевые ячейки:**
```python
# Загрузка модели
yamnet_model = hub.load('https://tfhub.dev/google/yamnet/1')

# Анализ аудио
results = audio_analyzer.analyze_audio('data/audio/demo_nature.wav')
```

#### 3️⃣ **03_visualization_alerts.ipynb**
**Что внутри:**
- Генерация тестовых данных
- Визуализация трендов
- Система оповещений
- Экспорт отчётов

**Время выполнения:** 2-3 минуты

**Ключевые ячейки:**
```python
# Генерация данных
monitoring_data = generate_test_monitoring_data(days=30)

# Создание системы оповещений
alert_system = AlertSystem()
alerts = alert_system.analyze(monitoring_data)
```

## 📂 Структура проекта

```
ecosystem_monitor/
├── 📘 README.md                    ← Полная документация
├── ⚡ QUICKSTART.md                ← Этот файл
├── 📖 USAGE.md                     ← Детальное руководство
├── 🤖 MODELS.md                    ← Описание ML моделей
├── 📦 requirements.txt             ← Зависимости Python
├── ⚙️  config.py                   ← Конфигурация
├── 📓 01_ecosystem_monitoring.ipynb  ← Анализ видео
├── 📓 02_audio_analysis.ipynb        ← Анализ аудио
├── 📓 03_visualization_alerts.ipynb  ← Визуализация
└── data/                            ← Данные
    ├── videos/                      ← Видеофайлы
    ├── audio/                       ← Аудиозаписи
    └── results/                     ← Результаты
```

## 🎓 Типичные сценарии использования

### Сценарий 1: Анализ собственного видео

```python
# 1. Поместите видео в data/videos/
# 2. В notebook выполните:

video_analyzer = VideoAnalyzer(model_yolo, confidence_threshold=0.3)

results = video_analyzer.analyze_video(
    video_path='data/videos/my_video.mp4',
    output_path='data/results/videos/analyzed.mp4',
    fps_sample=2
)

# 3. Результаты будут в results
print(f"Обнаружено животных: {len(results['detections'])}")
print(f"Виды: {results['statistics']}")
```

### Сценарий 2: Анализ собственного аудио

```python
# 1. Поместите аудио в data/audio/
# 2. В notebook выполните:

audio_results = audio_analyzer.analyze_audio(
    audio_path='data/audio/my_audio.wav',
    confidence_threshold=0.2
)

# 3. Просмотр результатов
for sound in audio_results['nature_sounds']:
    print(f"{sound['class_name']}: {sound['confidence']:.2%}")
```

### Сценарий 3: Создание отчёта

```python
# Генерация комплексного отчёта
export_comprehensive_report(
    df=monitoring_data,
    alerts=alerts,
    output_dir='data/results/reports'
)

# Отчёты будут сохранены в:
# - monitoring_data_YYYYMMDD_HHMMSS.csv
# - statistics_YYYYMMDD_HHMMSS.csv
# - alerts_YYYYMMDD_HHMMSS.csv
# - report_YYYYMMDD_HHMMSS.txt
```

## ⚙️ Настройка под свои задачи

### Изменение порога уверенности

```python
# Для видео (в config.py или напрямую)
YOLO_CONFIDENCE_THRESHOLD = 0.25  # По умолчанию
# 0.1-0.2: Больше детекций (больше ложных срабатываний)
# 0.3-0.5: Баланс
# 0.6-0.9: Меньше детекций (выше точность)

# Для аудио
AUDIO_CONFIDENCE_THRESHOLD = 0.3  # По умолчанию
```

### Выбор модели YOLO

```python
# В notebook измените:
model = YOLO('yolov8n.pt')  # Nano - самая быстрая

# На:
model = YOLO('yolov8s.pt')  # Small - баланс
model = YOLO('yolov8m.pt')  # Medium - хорошая точность
model = YOLO('yolov8l.pt')  # Large - высокая точность
model = YOLO('yolov8x.pt')  # XLarge - максимальная точность
```

### Настройка оповещений

Отредактируйте `config.py`:

```python
# Редкие виды, которые всегда генерируют оповещение
ALERT_RARE_SPECIES = ['bear', 'elephant', 'giraffe', 'zebra']

# Критическое снижение активности (50%)
ALERT_ACTIVITY_DROP = 0.5

# Необычно высокая активность (в 2 раза выше нормы)
ALERT_ACTIVITY_SPIKE = 2.0
```

## 🔧 Частые вопросы

### Q: Модель YOLOv8 не скачивается

**A:** При первом запуске модель скачивается автоматически. Если не работает:
```python
from ultralytics import YOLO
model = YOLO('yolov8n.pt')  # Автоматически скачает
```

### Q: Нехватка памяти при обработке видео

**A:** Увеличьте параметр `fps_sample`:
```python
# Вместо обработки каждого кадра (fps_sample=1)
results = video_analyzer.analyze_video(
    video_path='video.mp4',
    fps_sample=5  # Обрабатывать каждый 5-й кадр
)
```

### Q: TensorFlow долго грузится

**A:** Это нормально при первом запуске. TensorFlow инициализируется ~30 секунд.

### Q: Как обрабатывать большое количество файлов?

**A:** Используйте цикл:
```python
import glob

# Все видео в папке
video_files = glob.glob('data/videos/*.mp4')

for video_path in video_files:
    results = video_analyzer.analyze_video(video_path)
    # Обработка результатов
```

## 💡 Советы по производительности

### 🚀 Ускорение обработки

1. **Используйте GPU** (если доступен):
   ```python
   # YOLOv8 автоматически использует GPU
   # Проверка: model.device вернёт 'cuda' если GPU доступен
   ```

2. **Уменьшите разрешение видео**:
   ```python
   # Обработайте видео с меньшим разрешением
   VIDEO_RESIZE_FACTOR = 0.5  # В config.py
   ```

3. **Увеличьте fps_sample**:
   ```python
   # Вместо fps_sample=1 используйте 3-5
   fps_sample=3
   ```

### 🎯 Повышение точности

1. **Используйте большую модель**:
   ```python
   model = YOLO('yolov8m.pt')  # Вместо yolov8n
   ```

2. **Уменьшите fps_sample**:
   ```python
   fps_sample=1  # Обрабатывать каждый кадр
   ```

3. **Настройте порог уверенности**:
   ```python
   confidence_threshold=0.5  # Выше = строже
   ```

## 📊 Ожидаемые результаты

### Видеоанализ
- ✅ Обнаружение 10+ видов животных
- ✅ Точность 70-90% (зависит от качества видео)
- ✅ Скорость обработки: 2-10 FPS (зависит от железа)

### Аудиоанализ
- ✅ Распознавание 521 класса звуков
- ✅ Выделение природных звуков с точностью 60-80%
- ✅ Обработка аудио в реальном времени

### Визуализация
- ✅ Интерактивные графики Plotly
- ✅ Тепловые карты активности
- ✅ Временные ряды популяций

### Оповещения
- ✅ Детекция аномалий
- ✅ Мониторинг редких видов
- ✅ Экспорт отчётов в CSV/TXT

## 📧 Поддержка

Если возникли проблемы:

1. Проверьте, что все библиотеки установлены:
   ```bash
   pip list | grep -E "ultralytics|tensorflow|opencv"
   ```

2. Обновите библиотеки:
   ```bash
   pip install --upgrade ultralytics tensorflow opencv-python
   ```

3. Проверьте логи в notebooks (вывод ячеек)

4. Создайте Issue в репозитории проекта

## 🎉 Готово!

Вы готовы начать мониторинг экосистемы!

**Следующие шаги:**
1. Запустите все три notebook последовательно
2. Изучите сгенерированные отчёты
3. Загрузите свои видео и аудио
4. Настройте параметры под свои задачи
5. Создавайте отчёты для экологических исследований

---

**Хорошего мониторинга!**

*Проект создан для задачи "Мониторинг экосистемы через IT-решения"*
*Версия: 1.0.0*
*Дата: Февраль 2026*
