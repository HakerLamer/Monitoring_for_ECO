# 📘 Руководство по использованию

## Быстрый старт

### Шаг 1: Установка зависимостей

```bash
# Создание виртуального окружения (рекомендуется)
python -m venv venv

# Активация виртуального окружения
# На Linux/Mac:
source venv/bin/activate
# На Windows:
venv\Scripts\activate

# Установка библиотек
pip install -r requirements.txt
```

### Шаг 2: Запуск Jupyter Notebook

```bash
# Запуск Jupyter
jupyter notebook
```

### Шаг 3: Работа с notebooks

Откройте notebooks в следующем порядке:

1. **01_ecosystem_monitoring.ipynb** - Основной анализ видео с YOLOv8
2. **02_audio_analysis.ipynb** - Анализ аудио с YAMNet
3. **03_visualization_alerts.ipynb** - Визуализация и система оповещений

## 🎥 Работа с видео

### Подготовка видеофайлов

1. Поместите видеофайлы в директорию `data/videos/`
2. Поддерживаемые форматы: MP4, AVI, MOV
3. Рекомендуемое разрешение: 1280x720 или выше

### Анализ видео

```python
from ultralytics import YOLO

# Загрузка модели
model = YOLO('yolov8n.pt')

# Создание анализатора
video_analyzer = VideoAnalyzer(model, confidence_threshold=0.3)

# Анализ видео
results = video_analyzer.analyze_video(
    video_path='data/videos/my_video.mp4',
    output_path='data/results/videos/analyzed_video.mp4',
    fps_sample=2  # Обрабатывать каждый 2-й кадр
)
```

### Параметры анализа

- `confidence_threshold` (0.0-1.0): Минимальный порог уверенности детекции
  - 0.1-0.2: Более чувствительный (больше ложных срабатываний)
  - 0.3-0.5: Рекомендуемый баланс
  - 0.6-0.9: Более строгий (меньше детекций)

- `fps_sample` (1-30): Обработка каждого N-го кадра
  - 1: Максимальная точность (медленно)
  - 2-5: Рекомендуемый баланс
  - 10+: Быстрая обработка (низкая точность)

## 🎵 Работа с аудио

### Подготовка аудиофайлов

1. Поместите аудиофайлы в директорию `data/audio/`
2. Поддерживаемые форматы: WAV, MP3, OGG, FLAC
3. Рекомендуемая частота: 16000 Hz (модель автоматически конвертирует)

### Анализ аудио

```python
import tensorflow_hub as hub

# Загрузка модели YAMNet
yamnet_model = hub.load('https://tfhub.dev/google/yamnet/1')

# Создание анализатора
audio_analyzer = AudioAnalyzer(
    model=yamnet_model,
    class_names=class_names,
    nature_classes=nature_classes
)

# Анализ аудио
results = audio_analyzer.analyze_audio(
    audio_path='data/audio/my_audio.wav',
    confidence_threshold=0.2
)
```

## 📊 Визуализация результатов

### Создание графиков

```python
# Временные ряды
fig = px.line(df, x='date', y='count', color='species')
fig.show()

# Тепловая карта
sns.heatmap(pivot_data, cmap='YlOrRd')
plt.show()

# Интерактивный дашборд
dashboard = create_comprehensive_dashboard(data, alerts)
dashboard.show()
```

## 🚨 Система оповещений

### Настройка критериев

Отредактируйте файл `config.py`:

```python
# Минимальное количество животных для оповещения
ALERT_MIN_ANIMALS = 10

# Максимальное количество животных
ALERT_MAX_ANIMALS = 100

# Редкие виды (всегда генерируют оповещение)
ALERT_RARE_SPECIES = ['bear', 'elephant', 'giraffe']

# Критическое снижение активности (50%)
ALERT_ACTIVITY_DROP = 0.5

# Необычно высокая активность (в 2 раза)
ALERT_ACTIVITY_SPIKE = 2.0
```

### Запуск анализа

```python
# Создание системы оповещений
alert_system = AlertSystem()

# Анализ данных
alerts = alert_system.analyze(monitoring_data)

# Экспорт оповещений
alert_system.export_alerts('data/results/reports/alerts.csv')
```

## 💾 Экспорт результатов

### Форматы экспорта

1. **CSV** - табличные данные
2. **JSON** - структурированные данные
3. **TXT** - текстовые отчёты
4. **PNG/JPG** - визуализации

### Пример экспорта

```python
# Экспорт DataFrame в CSV
df.to_csv('data/results/reports/monitoring_data.csv', index=False)

# Экспорт статистики
stats.to_excel('data/results/reports/statistics.xlsx')

# Сохранение графика
plt.savefig('data/results/reports/chart.png', dpi=300)
```

## 🔧 Решение проблем

### Проблема: Модель YOLOv8 не скачивается

**Решение:**
```python
from ultralytics import YOLO
model = YOLO('yolov8n.pt')  # Автоматически скачает при первом запуске
```

Если не помогло, скачайте вручную:
```bash
wget https://github.com/ultralytics/assets/releases/download/v0.0.0/yolov8n.pt
```

### Проблема: TensorFlow не находит модель YAMNet

**Решение:**
```python
# Убедитесь, что установлен tensorflow-hub
pip install tensorflow-hub

# Модель скачается автоматически при первом запуске
yamnet_model = hub.load('https://tfhub.dev/google/yamnet/1')
```

### Проблема: Нехватка памяти при обработке видео

**Решение:**
```python
# Уменьшите FPS или разрешение
video_analyzer.analyze_video(
    video_path='video.mp4',
    fps_sample=5,  # Увеличьте число
    # Или уменьшите разрешение вручную перед обработкой
)
```

### Проблема: OpenCV не может открыть видео

**Решение:**
```bash
# Установите дополнительные кодеки
pip install opencv-python-headless

# Или конвертируйте видео в MP4
ffmpeg -i input.avi -c:v libx264 output.mp4
```

## 🎓 Примеры использования

### Пример 1: Мониторинг птиц

```python
# Анализ видео с птицами
results = video_analyzer.analyze_video('birds.mp4')

# Фильтрация только птиц
df = video_analyzer.get_statistics_df()
birds_only = df[df['class_name'] == 'bird']

# Визуализация
plt.plot(birds_only['timestamp'], birds_only.groupby('timestamp').size())
plt.title('Активность птиц по времени')
plt.show()
```

### Пример 2: Анализ ночных звуков

```python
# Анализ аудио
results = audio_analyzer.analyze_audio('night_sounds.wav')

# Фильтрация природных звуков
nature_sounds = [s for s in results['classifications'] if s['is_nature']]

# Вывод результатов
for sound in nature_sounds:
    print(f"{sound['class_name']}: {sound['confidence']:.2%}")
```

### Пример 3: Комплексный мониторинг

```python
# 1. Анализ видео
video_results = video_analyzer.analyze_video('wildlife.mp4')

# 2. Анализ аудио
audio_results = audio_analyzer.analyze_audio('wildlife_audio.wav')

# 3. Объединение данных
combined_df = pd.DataFrame({
    'video_detections': [len(video_results['detections'])],
    'audio_detections': [len(audio_results['nature_sounds'])],
    'unique_species': [video_results['unique_species']]
})

# 4. Генерация отчёта
export_comprehensive_report(combined_df, alerts)
```

## 📚 Дополнительные ресурсы

- [Документация YOLOv8](https://docs.ultralytics.com/)
- [TensorFlow Hub - YAMNet](https://tfhub.dev/google/yamnet/1)
- [Librosa Documentation](https://librosa.org/doc/latest/)
- [Plotly Documentation](https://plotly.com/python/)

## 💡 Советы по оптимизации

1. **Для больших видео:**
   - Используйте `fps_sample >= 3`
   - Уменьшайте разрешение видео
   - Обрабатывайте видео фрагментами

2. **Для повышения точности:**
   - Используйте модель `yolov8m` или `yolov8l` вместо `yolov8n`
   - Уменьшите `fps_sample` до 1
   - Увеличьте `confidence_threshold`

3. **Для ускорения:**
   - Используйте GPU (автоматически если доступен)
   - Увеличьте `fps_sample`
   - Используйте `yolov8n` (самая быстрая модель)

## 📧 Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории проекта.
